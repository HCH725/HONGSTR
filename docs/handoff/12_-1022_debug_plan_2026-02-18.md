# -1022 Debug Plan (Evidence-First, One-Variable Changes)

- Date: 2026-02-18
- Scope: futures testnet POST signature mismatch only.
- Rule: no signing refactor; no library switch; no multi-variable edits.

## 1) Evidence to Collect Every Run (`--debug_signing`)

For each failing POST attempt, capture and persist (redacted):
- `Pre-Sign QS` exact string.
- `Final URL` with redacted signature.
- `prepared_body_is_empty` must be `True`.
- prepared headers must include `X-MBX-APIKEY`.
- server vs local time delta (ms).
- endpoint/method/params snapshot: `symbol`, `side`, `type`, `quantity`, `timestamp`, `recvWindow`.

Suggested evidence log target:
- `reports/signing_debug_latest.md` (append run blocks with UTC timestamp).

## 2) Known-Good Control Experiment (No change to current signer)

Goal:
- Compare our signer against official/known-good request for the same endpoint and identical params.

Control matrix:
1. `GET /fapi/v2/account` signed (should pass).
2. `POST /fapi/v1/order` signed dry fixture (same params as failing path).

Compare fields:
- canonical param ordering,
- canonical pre-sign query string,
- resulting signature bytes/hex,
- request transport (`params=None`, `data=None`, empty body).

Acceptance:
- if QS and signature match known-good but API still returns `-1022`, isolate transport/body/time.

## 3) One-Variable Change Matrix

Run sequence (change one variable only; re-run smoke each time):

1. Timestamp source
- A: local `int(time.time()*1000)`
- B: server-synced timestamp (`serverTime + measured offset`)

2. `recvWindow`
- A: default omitted
- B: explicit `5000`
- C: explicit `10000`

3. Transport/body strictness
- A: `requests.request(..., params=None, data=None, json=None)`
- B: enforce no `Content-Type` header
- C: verify prepared body stays truly empty (not `""`)

4. Quantity formatting
- A: current float stringify
- B: normalized decimal string (no scientific notation, no trailing noise)

5. URL encoding edge cases
- A: current `urlencode(sorted_items, doseq=True)`
- B: same encoder with explicit type normalization for bool/float/string prior to sign

Stop condition for each row:
- If row changes behavior (200 vs -1022), freeze and document root cause before any next change.

## 4) Repro Tool (Allowed Minimal Addition)

If needed, add one minimal script only:
- `scripts/repro_signing_post_order.py`

Requirements:
- fixed fixture params,
- `--debug_signing` output,
- no order send unless explicit `--send`,
- testnet-only guard,
- writes evidence snapshot to `reports/`.

## 5) Execution Order (after Objective 1 stabilization)

1. Make `exchange_smoke_test` and `execute_paper --debug_signing` reproducible and deterministic.
2. Run known-good control against same params.
3. Execute one-variable matrix.
4. Only after stable pass: generate dry-run orders plan.
5. Only then optional tiny `--send` testnet order with notional cap.
